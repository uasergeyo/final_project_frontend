const initialState = {
	// jwt_token:null,
	// userData:null,
	// pending:false,
	// redirectedFrom:null,
	// announcements:null,
	// "login", "redirectedFromLink", "promiseReducer".
	
};

export default initialState;